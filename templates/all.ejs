<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Links</title>
</head>
<body>
    <!--indice recebe 0 , enquanto indice for menor que links.lenght(quantidade de links)
    faça-->
    <% for(let i = 0; i < links.length; i++) {%>
        <div id = "<%= links[i].id %> ">
            <h2><%= links[i].title %> </h2>
            <div><%= links[i].description %> </div>
            <div><%= links[i].url %> </div>
            <button onclick="removeLink('<%= links[i].id %>')" class="btn btn-dark">Deletar</button>
        </div>
            
    <%}%>


    <script>
        function removeLink(id) {
            
            fetch("/" + id, {method: "DELETE"}).then( result=> {
                result.text().then(linkId => {
                    console.log(linkId)
                    document.getElementById(linkId).remove();
                })
            })
        }
    </script>
    
    
</body>
</html>